const express = require('express');
const router = express.Router();
const db = require('./models/reportModel');



router.post('/', (req, res, next) => {
// send back custome feed
});

module.exports = router;